#!/bin/sh
# This file is generated by catkin. 

export PYTHONPATH=@CMAKE_BINARY_DIR@/gen/py:$PYTHONPATH
export PATH=@CMAKE_BINARY_DIR@/bin:@catkin_SOURCE_DIR@/bin:$PATH
export CATKIN_BINARY_DIR=@CMAKE_BINARY_DIR@
export CATKIN_SOURCE_DIR=@CMAKE_SOURCE_DIR@
if [ -z "$CATKIN_SHELL" ] ; then
   CATKIN_SHELL=sh
fi

FIND=`which find`
for envfile in `$FIND $CATKIN_BINARY_DIR/etc/catkin/profile.d -maxdepth 1 -name "*.all" 2>/dev/null` `$FIND $CATKIN_BINARY_DIR/etc/catkin/profile.d -maxdepth 1 -name "*.$CATKIN_SHELL" 2>/dev/null`
do
  . $envfile
done

