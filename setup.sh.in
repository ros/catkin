#
# PREFIX/lib/pythonX.Y/dist-packages is the way it looks under --install-layout=deb
#
_PYVER=$(python -c 'from sys import version_info as v; print "%u.%u" % (v.major, v.minor)')
PYTHONPATH=@CMAKE_INSTALL_PREFIX@/lib/python${_PYVER}/dist-packages:$PYTHONPATH
unset _PYVER


